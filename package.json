{
  "package": "wgpu",
  "devDependencies": {
    "@napi-rs/cli": "^1.0.0"
  },
  "napi": {
    "name": "binding"
  },
  "scripts": {
    "build": "napi build --release",
    "build:debug": "napi build"
  }
}
